Patching mysql database server

Windows
# Cd to mysql.exe ( C:\Program Files\MySQL\MySQL Server 5.x\bin )
# Log on to the database ( mysql.exe -u root -p )
# Execute the sqlfile ( source <sqlfilename> )
# Or run it all at the commandline : mysql game_unitybeer -u root -p < 0002_UtilityFunctions.sql

Linux
# Log on to the database ( mysql.exe -u root -p )
# Execute the sqlfile ( source <sqlfilename> )
# Or run it all at the commandline : mysql game_unitybeer -u root -p < 0002_UtilityFunctions.sql

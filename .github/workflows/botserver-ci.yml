name: Bot Server - CI Docker Container

on:
  push:
  workflow_dispatch:

env:
  IMAGE_NAME: botwebserverimage

jobs:
  build:
    name: Build Docker Image
    uses: DarkFactorAS/Infrastructure/.github/workflows/infrastructure-server-ci.yml@v1.1
    with:
      project_path: "BotWebServer"
      project_name: "BotWebServer"
    secrets: inherit